# Baked into `npm run build` / `npm run package` by Vite (see src/online/hostUrl.ts).
# The deployed PartyKit room (packages/server, `npm run server:deploy`).
# A public hostname, not a secret — safe to commit.
# The release workflow's `VITE_PARTYKIT_HOST` repo variable overrides this when set.
VITE_PARTYKIT_HOST=boomtown.smromain.partykit.dev
